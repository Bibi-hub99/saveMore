function HeroText(props){

    //hero text which acts as logo of the company
    return (
        <h1 className={`font-bold ${props.heroStyle} md:text-2xl`}>save More</h1>
    )

}

export default HeroText